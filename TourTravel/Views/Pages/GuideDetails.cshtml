@model TourTravel.Models.TourGuideView
@await Component.InvokeAsync("HeroSection")
<!-- team single -->
<div class="team-single pt-120 pb-80">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-4">
                <div class="team-single-img">
                    <img src="@Model.ImageUrl" alt="@Model.Name">
                </div>
            </div>
            <div class="col-md-8">
                <div class="team-details">
                    <h3>@Model.Name</h3>
                    <p class="mb-3 team-tagline"><strong>@Model.Role</strong></p>
                    <p>@Html.Raw(Model.Introduction)</p>
                    <div class="team-details-info">
                        <ul>
                            <li><a href="#"><i class="far fa-envelope"></i> @Model.Email</a></li>
                            <li><a href="#"><i class="far fa-phone"></i> @Model.Phone</a></li>
                        </ul>
                    </div>
                    <div class="team-details-social">
                        @if (!string.IsNullOrEmpty(@Model.Facebook))
                        {
                            <a href="@Model.Facebook" target="_blank"><i class="fab fa-facebook-f"></i></a>
                        }
                        @if (!string.IsNullOrEmpty(@Model.Twitter))
                        {
                            <a href="@Model.Twitter" target="_blank"><i class="fab fa-twitter"></i></a>
                        }
                        @if (!string.IsNullOrEmpty(@Model.Instagram))
                        {
                            <a href="@Model.Instagram" target="_blank"><i class="fab fa-instagram"></i></a>
                        }
                        @if (!string.IsNullOrEmpty(@Model.LinkedIn))
                        {
                            <a href="@Model.LinkedIn" target="_blank"><i class="fab fa-linkedin"></i></a>
                        }
                        @if (!string.IsNullOrEmpty(@Model.YouTube))
                        {
                            <a href="@Model.YouTube" target="_blank"><i class="fab fa-youtube"></i></a>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- team single end -->
<!-- biography & skill -->
<div class="biography-skill pb-120">
    <div class="container">
        <div class="row">
            <div class="col-md-7 mb-3">
                <div class="biography">
                    <h4 class="mb-3">Biography</h4>
                    <p class="mb-10">
                        @Html.Raw(Model.Biography)
                    </p>
                </div>
            </div>
            <div class="col-md-5">
                <div class="biography-img">
                    <img src="@Model.ImageUrl" alt="@Model.Name">
                </div>
            </div>
        </div>
    </div>
</div>
<!-- biography & skill end --> 