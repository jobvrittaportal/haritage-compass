@model IEnumerable<TourTravel.Models.Testimonial>
@{
    ViewData["Title"] = "Testimonial";
    Layout = "~/Admin/Views/Shared/_Layout-common.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between mb-3 align-items-center">
        <h3>Testimonial</h3>
        <a href="@Url.Action("Create", "Testimonial")" class="btn btn-success">
            <i class="fa-solid fa-plus me-1"></i> Add New
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-dark">
                <tr>
                    <th>AuthorName</th>
                    <th>Role</th>
                    <th>Rating</th>
                    <th>Quote</th>
                    <th>Image</th>
                    <th style="width: 130px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.AuthorName</td>
                            <td>@item.Role</td>
                            <td>@item.Rating</td>
                            <td>@item.Quote</td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.ImageUrl))
                                {
                                    <img src="@item.ImageUrl" alt="Image" style="width:100px;height:60px;object-fit:cover;" />
                                }
                                else
                                {
                                    <span class="text-muted">No image</span>
                                }
                            </td>
                            <td class="text-center">
                                <!-- Edit Button -->

                                <a href="@Url.Action("Edit", "Testimonial", new { id = item.Id })"
                                   class="btn btn-icon btn-sm btn-warning me-1"
                                   title="Edit">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>


                                <!-- Delete Button -->
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post"
                                      class="d-inline-block" onsubmit="return confirm('Are you sure?');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-icon btn-sm btn-danger" title="Delete">
                                        <i class="fa-solid fa-trash"></i>
                                    </button>
                                </form>
                            </td>

                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted">No Image found.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>

       $(function () {
                Initialize Bootstrap tooltips
                $('[data-bs-toggle="tooltip"]').tooltip();
            });
    </script>
}
